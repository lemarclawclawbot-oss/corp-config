---
type: handoff
timestamp: 2026-04-05T20:00:00-05:00
---

# Session Handoff — ZBook Corp Setup

## Paste this into new Claude Code session to resume:

---

You are resuming a ZBook corp AI fleet setup session. Here's the exact state:

**Machines:**
- ZBook (100.123.233.45) — lead agent, you run here, all services live here
- Lenovo (100.66.197.79, user: lem-ai) — SSH key installed, reachable from ZBook
- Chromebook (100.82.232.25, user: lemai) — Tailscale offline, needs `sudo tailscale up` on device

**Running Services on ZBook:**
- Mission Control dashboard: http://100.123.233.45:5000
- Corp Dispatch (phone command interface): http://100.123.233.45:5001 (PIN: 096361)
- corp-observer, corp-dashboard, corp-dispatch (all systemd services)
- Ollama: localhost:11434 — models: glm4:latest, qwen2.5-coder:7b, hermes3:latest

**What was just built/fixed this session:**
1. model-switch script fixed (was corrupting settings.json with invalid JSON via sed)
2. settings.json now correctly set to claude-opus-4-6
3. Aider installed at ~/.aider-venv — wrappers: `aider`, `aider-glm5`, `aider-local`
4. Lenovo SSH working from ZBook (`ssh lem-ai@100.66.197.79`)
5. Lenovo model-switch configured to route to ZBook Ollama (100.123.233.45:11434)
6. Dispatch PATH fix applied — dispatch/app.py now uses `PATH=$HOME/.local/bin:$PATH` in SSH commands so remote claude binary is found
7. tmux installed on ZBook and Lenovo

**Immediate TODOs (need sudo):**
1. `sudo systemctl restart corp-dispatch` — apply the PATH fix so Lenovo dispatch works
2. `sudo systemctl daemon-reload` — clear the unit file warning
3. Chromebook: physically go to it and run `sudo tailscale up`

**Architecture (Lemar's vision confirmed working):**
- Write ALL code on ZBook in this session
- Push to Lenovo via SSH or git (no need for separate Claude session there)
- Dispatch app lets Lemar send commands to any machine FROM HIS PHONE
- Only ZBook needs a Claude Code session — saves 2/3 of API costs
- When session limits hit: use `aider` (same API key, no session wall) or `aider-local` (free Ollama)

**Key file locations:**
- Corp config: ~/corp-config/
- Dispatch: ~/corp-config/dispatch/app.py
- Model switch: ~/.local/bin/model-switch
- Aider wrappers: ~/.local/bin/aider, ~/.local/bin/aider-glm5, ~/.local/bin/aider-local
- Settings: ~/.claude/settings.json (currently: claude-opus-4-6)

**Pending items not yet addressed:**
- Chromebook claude install (needs Tailscale up first, then SSH in and install)
- Add model-switch capability to Dispatch UI (let phone switch models)
- Terminal crash prevention (tmux installed, need to make it default shell startup)
- VSCode/Cursor IDE evaluation (user wants to explore this for coding agents)
- HP BIOS call: 1-800-474-6836, Serial CND24534YK

